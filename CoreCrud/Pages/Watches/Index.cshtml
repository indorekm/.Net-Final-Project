@page
@model CoreCrud.Pages.Watches.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Watch[0].Brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Watch[0].LaunchDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Watch[0].IsAnalog)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Watch[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Watch[0].Material)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Watch[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Watch[0].Manufacturer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Watch) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Brand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LaunchDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsAnalog)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Material)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Manufacturer.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
